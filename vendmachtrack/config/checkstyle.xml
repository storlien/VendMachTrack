<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">

    <!-- File Tab Character -->
    <module name="FileTabCharacter"/>

    <module name="TreeWalker">
        <!-- Indentation -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="lineWrappingIndentation" value="4"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- Trailing Comment -->
        <module name="TrailingComment"/>

        <!-- Redundant Import -->
        <module name="RedundantImport"/>

        <!-- Unused Imports -->
        <module name="UnusedImports"/>

        <!-- Empty Block -->
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>

        <!-- Need Braces -->
        <module name="NeedBraces"/>

        <!-- Empty Line Separator -->
        <module name="EmptyLineSeparator">
            <property name="tokens" value="PACKAGE_DEF,IMPORT,CLASS_DEF,INTERFACE_DEF,ENUM_DEF"/>
        </module>

        <!-- No Whitespace Before -->
        <module name="NoWhitespaceBefore">
            <property name="tokens" value="DOT,SEMI,POST_INC,POST_DEC"/>
        </module>

        <!-- No Whitespace After -->
        <module name="NoWhitespaceAfter">
            <property name="tokens" value="INC,DEC"/>
        </module>

        <!-- Whitespace Around -->
        <module name="WhitespaceAround">
            <property name="tokens" value="ASSIGN,LE,GE,NOT_EQUAL"/>
        </module>

    </module>

    <!-- Line Length -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>
</module>
